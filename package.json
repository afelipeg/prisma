{
  "name": "prism",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "demo": "bun run --cwd packages/cli src/index.ts demo",
    "mcp": "bun run --cwd packages/cli src/index.ts mcp",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
